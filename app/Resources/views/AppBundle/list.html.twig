{% extends 'base.html.twig' %}

{% block body %}
    
    {% for message in app.flashes('notice') %}
        <div class="flash-notice">
            {{ message }}
        </div>
    {% endfor %}
    <div>
    {{ grid_search(grid, ':AppBundle:grid.html.twig') }}
    {{ grid(grid, ':AppBundle:grid.html.twig') }}
    </div>
    
{% endblock %}

